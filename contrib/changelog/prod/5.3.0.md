* GUI package is now installed by default
* Added nordvpn diagnostics command
* Added pause command support in the cli
* Added button to invoke GUI from tray
* Allowlist mDNS by default when LAN discovery is enabled
* Make cli commands case insensitive
* Add option to disable ECH in NordWhisper
* We increased glibc version to 2.35
* We dropped support for Ubuntu 20.04, Debian 11, Fedora 32-42, now oldest supported versions are: Ubuntu 22.04 , Fedora 43  and Debian 12
