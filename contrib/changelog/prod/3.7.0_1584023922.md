What's new:
* Now you can restore default settings by using the `nordvpn set defaults` command.
* Access resources (computers, printers, TVs – you name it) while using VPN – now no matter on what network you’re on.
 Use the `nordvpn whitelist add subnet` command to specify subnet addresses.
